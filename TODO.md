# Deployment to Vercel - TODO

- [x] Inspect current entrypoints/config (server.js, package.json, vercel.json)
- [x] Update vercel.json rewrites to avoid breaking /api/* while keeping SPA fallback
- [x] Basic config sanity checks (vercel.json updated)
- [ ] Deploy to Vercel and test API + SPA routes
- [ ] If data persistence breaks, replace file-based db with persistent storage (Vercel KV / external DB)


